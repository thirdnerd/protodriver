# TI-84 Plus CE module

This package declares the one read-only screenshot operation admitted from the
retained TI-84 Plus CE exchange. It is intentionally not a general calculator
module. The package-root `device.lua` is authoritative; `artifacts/` contains
its reproducible contract-2 package, public description, and authored identity.

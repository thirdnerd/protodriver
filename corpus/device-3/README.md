# Device 3 module

The authoritative source set is the package-root `device.lua`; `artifacts/`
contains its reproducible contract-2 package, public description, and authored
identity.

The module describes the revision-1 application protocol over serial and USB,
including device information, diagnostics, and durable bulk-image transfer.
